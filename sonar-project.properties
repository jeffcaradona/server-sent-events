# SonarCloud Configuration
sonar.projectKey=jeffcaradona_server-sent-events
sonar.organization=xbe90seup2ltm6gwmugkq

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=server-sent-events
sonar.projectVersion=0.0.1

# Path is relative to the sonar-project.properties file. Defaults to .
sonar.sources=src
sonar.tests=test

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Coverage information
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Exclusions for coverage
sonar.coverage.exclusions=**/test/**,**/bin/**,**/public/**,**/views/**,**/node_modules/**

# Test exclusions
sonar.test.inclusions=test/**/*.test.js
